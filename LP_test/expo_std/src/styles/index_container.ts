import { StyleSheet } from "react-native"


export const style = StyleSheet.create({
    text:{
        color: 'blue',
        fontSize: 32,
        fontWeight: 'bold'
    },
    container:{
        alignItems: 'center',
        justifyContent: 'center',
        flex: 1,
        padding: 42,

    }
})
