num=float(input("digite um número para ser somado, ou  Zero para encerar: "))
contador=0

while num != 0:
    contador+=num
    num=float(input("digite um número para ser somado, ou  Zero para encerar: "))

print("a soma dos números digitados é: ", contador)
    